DefineScene.ArenaEmpty = {
    entities = {
        { prefab = Entities.Camera, pose_overrides = { position = Vector2(0, 0) } },
        { prefab = Entities.Player, pose_overrides = { position = Vector2(0, 0) } },
    },
}
